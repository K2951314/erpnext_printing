[project]
name = "erpnext_printing"
authors = [
    { name = "舍满取半", email = "qdsmqb@163.com" }
]
description = "Print formats and invoice import utilities for ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=16.0.0" # Installed and managed by bench.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"
